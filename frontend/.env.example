# Backend WebSocket address. Do not set it for local development (default ws://<hostname>:8000/ws).
# For a Vercel deployment, set it in the project Settings → Environment Variables:
VITE_WS_URL=wss://warehouse-twin-backend.onrender.com/ws
